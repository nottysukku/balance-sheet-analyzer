# Normalise to LF in the repository so the tree is identical on every platform.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.pdf   binary
*.xlsx  binary
*.xls   binary
*.png   binary
*.jpg   binary
*.ico   binary
